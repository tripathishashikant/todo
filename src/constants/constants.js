const THEME_SWITCHER = {
  light: 'light',
  dark: 'dark',
};

const LOCAL_STORAGE = {
  currentTheme: 'currentTheme',
};

const CONSTANTS = {
  THEME_SWITCHER,
  LOCAL_STORAGE,
};

export default CONSTANTS;
